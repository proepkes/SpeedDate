apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mmsvcSettings.ImageName }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.mmsvcSettings.ImageName }}
spec:
  ports:
  - port: 8001
    targetPort: {{ .Values.mmsvcSettings.Port }}
    name: http
  selector:
    app: {{ .Values.mmsvcSettings.ImageName }}
