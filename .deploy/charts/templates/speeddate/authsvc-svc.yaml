apiVersion: v1
kind: Service
metadata:
  name: authsvc
  namespace: {{ .Release.Namespace }}
  labels:
    app: authsvc
spec:
  ports:
  - port: 8001
    targetPort: {{ .Values.authsvcSettings.Port }}
    name: http
  selector:
    app: authsvc
