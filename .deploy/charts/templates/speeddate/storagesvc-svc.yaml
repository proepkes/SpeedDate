apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.storagesvcSettings.ImageName }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.storagesvcSettings.ImageName }}
spec:
  ports:
  - port: 8001
    targetPort: {{ .Values.storagesvcSettings.Port }}
    name: http
  selector:
    app: {{ .Values.storagesvcSettings.ImageName }}
