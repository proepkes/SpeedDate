namespace SpeedDate.Network
{
    public enum ResponseStatus
    {
        Default = 0,
        Success,
        Timeout,
        Error,
        Unauthorized,
        Failed,
        NotConnected,
        NotHandled
    }
}
